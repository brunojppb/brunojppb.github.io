[package]
name = "blog"
version = "0.1.0"
edition = "2024"

[dependencies]
maudit = "0.12"
maud = "0.27"
serde = "1.0"
chrono = { version = "0.4", features = ["serde"] }

[profile.dev.package."*"]
opt-level = 3
